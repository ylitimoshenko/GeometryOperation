package test.java;

public class CircleOperations {

    public double valueOfCilinder(int r, int h) {
        return Math.PI * (r * r * h);
    }

    public double squareOfCircle(int r) {
        return Math.PI * (r * r);
    }
}
